# Backend rules

- Each route group should have `routes.ts`, `service.ts`, `repository.ts`, `schemas.ts`, and tests.
- Validate request boundaries with shared schemas.
- Use centralized runtime path helpers.
- Keep route handlers thin.
